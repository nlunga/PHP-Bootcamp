<?php
/*  $count = 1;
  while ($count < $argc) {
    $temp[$count] = trim($argv[$count]);
    if (preg_replace())
  }
  sort($temp);
  foreach ($temp as $temp) {
    echo preg_replace('/ /');
  }*/
  $input = $argv[1];
  if (stripos($input, ' ') !== false) {
    echo 'ZOMG I HAS WORDS';
  }
?>
